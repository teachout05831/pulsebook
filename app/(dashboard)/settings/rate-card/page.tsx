import { RateCardManager } from "@/features/estimate-pages";

export default function RateCardPage() {
  return (
    <div className="container mx-auto py-6 max-w-4xl">
      <RateCardManager />
    </div>
  );
}
