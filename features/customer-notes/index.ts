// Hooks
export { useCustomerNotes } from './hooks'

// Actions
export { createNote, updateNote, deleteNote } from './actions'

// Queries (server-only — import directly from ./queries)

// Types
export type { CustomerNote, CreateNoteInput, UpdateNoteInput, NoteActionResult } from './types'
